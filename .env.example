# Local dev: a file path works directly.
# Production (Turso): "libsql://<db-name>-<org>.turso.io?authToken=<token>"
DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-a-long-random-string"

# Required for the AI food-photo analysis feature (Nutrition page).
# Get a free key (no billing required) at https://console.groq.com/keys
GROQ_API_KEY=""
